import { cookies } from "next/headers";

export default function Page() {
  //   const token = cookies().get("token")?.value;

  // decode or fetch user info from backend
  //   const user = await getUserFromToken(token);

  return <div>ADMIN</div>;
}
